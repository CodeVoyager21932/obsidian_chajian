# CareerOS - 职业规划操作系统

CareerOS 是一个 Obsidian 插件，旨在构建一套「职业规划操作系统」。该系统通过分析用户在 Obsidian 中积累的笔记（项目、课程、反思等）和市场招聘信息（JD），自动生成能力画像、市场画像、差距分析和可执行的行动计划。

## ✨ 特性

- 📝 **自动提取技能信息** - 从项目笔记中提取技能、经验和偏好
- 🔄 **增量更新** - 笔记修改后自动同步能力画像
- 🔒 **隐私优先** - 支持本地 LLM，数据本地存储，外部调用自动 PII 过滤
- 📊 **能力画像** - 聚合技能数据，生成全面的自我画像
- 💼 **市场分析** - 从招聘信息中提取岗位要求
- 📈 **差距分析** - 对比自我画像和市场需求
- 🎯 **行动计划** - 生成可执行的能力提升计划
- 🎨 **可视化面板** - 集中查看所有数据和分析结果

## 📦 安装

### 开发环境设置

1. 克隆此仓库到 Obsidian vault 的 `.obsidian/plugins/career-os` 目录：
   ```bash
   cd your-vault/.obsidian/plugins
   git clone https://github.com/CodeVoyager21932/obsidian_chajian career-os
   ```

2. 安装依赖：
   ```bash
   cd career-os
   npm install
   ```

3. 构建插件：
   ```bash
   npm run build
   ```

4. 在 Obsidian 中启用插件：
   - 打开 Obsidian 设置
   - 进入「第三方插件」
   - 关闭「安全模式」
   - 点击「浏览」并启用 CareerOS

### 开发模式

运行开发模式（自动重新构建）：
```bash
npm run dev
```

## 🚀 快速开始

### 1. 配置 LLM

在插件设置中配置 LLM 提供商。支持以下选项：

| 提供商 | 说明 | 推荐场景 |
|--------|------|----------|
| **Local (Ollama)** | 本地运行，完全隐私 | 日常使用，敏感数据 |
| **OpenAI** | GPT-4/3.5 | 高质量分析 |
| **Anthropic** | Claude | 长文本处理 |
| **Google** | Gemini | 多模态支持 |

> ⚠️ **隐私提示**：使用外部 LLM 时，系统会自动过滤 PII（手机号、邮箱等），但建议敏感笔记使用本地 LLM。

### 2. 冷启动索引

首次使用时，运行冷启动索引来处理现有笔记：

1. 打开命令面板 (`Ctrl/Cmd + P`)
2. 搜索并执行 `CareerOS: Cold Start Indexing`
3. 等待索引完成（进度条会显示处理状态）

### 3. 构建能力画像

执行 `CareerOS: Build Self Profile` 命令，系统会：
- 聚合所有 NoteCard 中的技能信息
- 应用技能名称标准化（Taxonomy）
- 计算技能熟练度分数（考虑时间衰减）
- 生成 JSON 和 Markdown 格式的画像文件

### 4. 分析市场需求

1. 创建包含招聘信息的笔记（可以是多个 JD 的合集）
2. 执行 `CareerOS: Extract JD Cards from Current Note`
3. 执行 `CareerOS: Build Market Profile`

### 5. 生成差距分析和行动计划

1. 执行 `CareerOS: Generate Gap Analysis`
2. 执行 `CareerOS: Generate Action Plan`
3. 在 Dashboard 中查看结果

### 6. 查看面板

执行 `CareerOS: Open Dashboard` 打开可视化面板，集中查看：
- 技能概览和熟练度
- 最近项目
- 市场画像列表
- 差距分析和行动计划

## 📁 项目结构

```
career-os/
├── main.ts              # 插件主入口
├── types.ts             # TypeScript 类型定义
├── schema.ts            # Zod 验证模式和迁移逻辑
├── ProfileEngine.ts     # 核心提取和画像构建引擎
├── MarketScanner.ts     # JD 提取和市场画像构建
├── StrategyCore.ts      # 差距分析和行动计划生成
├── llmClient.ts         # 多 LLM 提供商统一接口
├── queue.ts             # 任务队列和并发控制
├── IndexStore.ts        # 索引文件读写和版本管理
├── fs.ts                # 原子文件操作服务
├── PrivacyGuard.ts      # PII 过滤和排除规则
├── Taxonomy.ts          # 技能名称标准化
├── PromptStore.ts       # Prompt 模板管理
├── logger.ts            # 错误日志系统
├── SettingsTab.ts       # 设置界面
├── prompts/             # Prompt 模板文件
│   ├── noteCardPrompt.txt
│   ├── jdCardPrompt.txt
│   └── planPrompt.txt
├── views/               # React 视图组件
│   ├── DashboardView.tsx
│   ├── DashboardContext.tsx
│   └── ProgressTracker.tsx
├── utils/               # 工具函数
│   ├── jsonCleaner.ts   # JSON 清洗器
│   └── errorLogParser.ts
└── styles/              # CSS 样式
    └── dashboard.css
```

## 📊 数据架构

CareerOS 使用三层数据架构：

```
┌─────────────────────────────────────────────────────────────┐
│                     原始层 (Raw Layer)                       │
│              Obsidian Vault 中的 Markdown 笔记               │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                     卡片层 (Card Layer)                      │
│         NoteCard (技能/项目)    JDCard (岗位要求)            │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                  画像/视图层 (Profile Layer)                 │
│    SelfProfile    MarketProfile    Gap Analysis    Plans    │
└─────────────────────────────────────────────────────────────┘
```

### 数据文件位置

| 数据类型 | 默认路径 | 说明 |
|----------|----------|------|
| NoteCard | `CareerOS/index/` | 从笔记提取的结构化卡片 |
| JDCard | `CareerOS/market_cards/` | 从 JD 提取的岗位卡片 |
| SelfProfile | `CareerOS/mapping/` | 聚合的自我画像 |
| MarketProfile | `CareerOS/mapping/` | 聚合的市场画像 |
| Gap Analysis | `CareerOS/mapping/` | 差距分析报告 |
| Action Plan | `CareerOS/mapping/` | 行动计划 |
| Error Log | `CareerOS/error_log.md` | 错误日志 |

## ⚙️ 配置选项

详细配置说明请参阅 [CONFIGURATION.md](./CONFIGURATION.md)。

### 核心配置

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `indexDirectory` | `CareerOS/index` | NoteCard 存储目录 |
| `mappingDirectory` | `CareerOS/mapping` | 画像和分析存储目录 |
| `marketCardsDirectory` | `CareerOS/market_cards` | JDCard 存储目录 |
| `maxRetries` | `3` | LLM 调用最大重试次数 |
| `timeout` | `30000` | LLM 调用超时时间 (ms) |
| `concurrency` | `3` | 并发处理任务数 |

### LLM 配置

系统支持为不同任务配置不同的 LLM：

- **extract**: 用于 NoteCard/JDCard 提取（推荐使用本地 LLM）
- **analyze**: 用于差距分析和计划生成（推荐使用高质量模型）
- **embedding**: 用于向量嵌入（预留）

## 🔧 命令列表

| 命令 | 说明 |
|------|------|
| `CareerOS: Cold Start Indexing` | 冷启动索引所有笔记 |
| `CareerOS: Extract JD Cards from Current Note` | 从当前笔记提取 JD |
| `CareerOS: Build Self Profile` | 构建自我画像 |
| `CareerOS: Build Market Profile` | 构建市场画像 |
| `CareerOS: Generate Gap Analysis` | 生成差距分析 |
| `CareerOS: Generate Action Plan` | 生成行动计划 |
| `CareerOS: Open Dashboard` | 打开可视化面板 |
| `CareerOS: View Error Log` | 查看错误日志 |

## 🔒 隐私保护

CareerOS 采用多层隐私保护机制：

1. **本地优先** - 所有数据存储在本地，无遥测或分析
2. **PII 过滤** - 外部 LLM 调用前自动过滤手机号、邮箱等
3. **目录排除** - 可配置排除特定目录或标签的笔记
4. **透明配置** - 外部 LLM 选项有明确警告提示

## 🐛 故障排除

详细故障排除指南请参阅 [TROUBLESHOOTING.md](./TROUBLESHOOTING.md)。

### 常见问题

**Q: 索引过程中出现大量错误**
- 检查 LLM 配置是否正确
- 查看 `error_log.md` 了解具体错误
- 尝试减少并发数

**Q: 技能名称不一致**
- 在设置中配置 Taxonomy 别名映射
- 重新构建 SelfProfile

**Q: 本地 LLM 连接失败**
- 确认 Ollama 服务正在运行
- 检查 base URL 配置

## 🧪 测试

运行测试：
```bash
npm test
```

运行单次测试（非 watch 模式）：
```bash
npm run test:run
```

## 📝 开发

### 构建

```bash
npm run build
```

### 类型检查

```bash
npx tsc --noEmit
```

### 代码风格

项目使用 TypeScript 严格模式，所有核心数据结构使用 Zod 进行运行时验证。

## 📄 许可证

MIT

## 🤝 贡献

欢迎贡献！请提交 Issue 或 Pull Request。

## 📮 支持

如有问题或建议，请在 [GitHub](https://github.com/CodeVoyager21932/obsidian_chajian) 上提交 Issue。
